const AuthConfig = {
    auth: {
        api : true,
        api_user: 'systech',
        api_pass: 'systech@s24',
        // play: true,
        // publish: true,
        // secret: 'nodemedia2017privatekey'
    }
}

export default AuthConfig
